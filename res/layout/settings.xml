<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
	android:orientation="vertical" android:layout_width="fill_parent"
	android:layout_height="fill_parent">
	<TextView android:text="Select refresh rate (seconds):"
		android:layout_width="wrap_content" android:layout_height="wrap_content"></TextView>
	<Spinner android:id="@+id/refresh" android:layout_width="wrap_content"
		android:layout_height="wrap_content" android:prompt="@string/refresh_prompt"></Spinner>
	<Button android:text="Add Database Connection" android:id="@+id/add"
		android:layout_width="fill_parent" android:layout_height="wrap_content"></Button>
	<Button android:text="Remove Database Connection" android:id="@+id/remove"
		android:layout_width="fill_parent" android:layout_height="wrap_content"></Button>
</LinearLayout>